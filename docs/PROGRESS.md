# フィジー配車アプリ — 開発状況

対象: `tukemen-rgb/fiji` / Draft PR #4 / `gdp/role-handoff-review-20260916`。

ユーザーの最新指示: 「引き続き作って、ループで」。利用者／運転手を分ける構成、シンプルなUI、GDPによる設計・試作・レビュー、Claudeによる本番実装という分担を維持する。

## 今回完了 — 現行切断と旧通知競合の一件再接続（2026-09-20・フィジー日付）

- 三つの運転手セッションを往復した後、現行購読の切断と旧二世代の保存済み通知コールバックを同時に発生させる統合受入を追加した。
- 現行世代だけを即時ロックして「通知を再接続する」を一件表示し、旧通知は認可済み処理前に `subscription_inactive` で拒否する。旧通知は現行ロックや案内を変更しない。
- 再接続ボタンを二重操作しても現行第三セッションの接続と認可済み最新状態照合を各一件だけ実行し、照合成功後にだけ状態変更ロックを解除する。
- 判定テスト347件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 偽イベント／DOMと依存注入サービスによるNode受入であり、実ブラウザの履歴・タップ、実認証SDK、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。
- 次の一項目は、再接続後の最新状態照合中に別アカウントへ切り替わった場合、旧照合を破棄して新セッションだけを有効化する受入とする。

## 今回完了 — 複数往復後の旧通知コールバック遮断（2026-09-20・フィジー日付）

- 三つの運転手セッションをログアウト／再ログインで往復した後、旧二世代の通知購読に保存されていたコールバックを再発火する統合受入を追加した。
- 旧コールバックは `subscription_inactive` として、認可済み通知処理 `handleNotificationHint` と追加の現在状態読取より前に拒否する。現行の第三セッションのコールバックだけを一件処理する。
- 旧通知を発火しても現行画面の状態変更ロック、DOMリスナー、役割ページを変更せず、公開状態へ旧アカウント参照を出さない。
- 判定テスト346件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 偽イベント／DOMと依存注入サービスによるNode受入であり、実ブラウザの履歴・タップ、実認証SDK、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。
- 次の一項目は、現行購読の切断と旧購読コールバック発火が重なっても、現行世代だけをロックして明示的な再接続一件へ収束する受入とする。

## 今回完了 — 複数回のログアウト／再ログインで一世代維持（2026-09-20・フィジー日付）

- 運転手セッションでログアウト→役割選択→別アカウント再ログインを二回繰り返す統合受入を追加した。
- 各ログアウトで直前の通知購読・起動時イベント・DOM操作を解除し、再ログイン後は現在アカウントの読取・購読だけを各一件開始する。再ログイン案内も各停止につき一件へ集約する。
- 三アカウントを往復しても、認証イベント、表示／オンラインイベント、DOMクリックの有効リスナーを各一件に維持し、状態変更ロックを現行画面で正しく解除する。公開状態にはアカウント参照を含めない。
- 判定テスト345件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 偽イベント／DOMと依存注入サービスによるNode受入であり、実ブラウザの履歴・タップ、実認証SDK、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。
- 次の一項目は、複数往復後に旧購読の保存済みコールバックが発火しても、認可済み読取前に拒否する受入とする。

## 今回完了 — 再ログイン後の第三セッション限定再入場（2026-09-19・フィジー日付）

- 旧アカウントから置換アカウントへの入場中に認証が消失した後、「役割選択へ戻る」を経て第三の新セッションで同じ運転手画面へ再入場する統合受入を追加した。
- 役割選択への離脱で再ログイン案内・旧DOMリスナー・状態変更ロックを解除し、第三セッションの現在状態読取と通知購読だけを各一件開始する。
- 旧二世代の読取が後から正常応答しても第三セッションの画面・購読を上書きせず、旧画面へ戻らない。公開状態には三セッションいずれのアカウント参照も含めない。
- 判定テスト344件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 偽イベント／DOMと依存注入サービスによるNode受入であり、実ブラウザの履歴・タップ、実認証SDK、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。
- 次の一項目は、ログアウト・再ログインを複数回繰り返しても、購読・認証・DOMリスナーを現行セッション一件に保つ受入とする。

## 今回完了 — 置換入場中の再認証消失を即時安全停止（2026-09-19・フィジー日付）

- 旧アカウントから新アカウントへの置換入場が現在状態読取を待っている途中で、新アカウントの認証も消失する統合受入を追加した。
- 保留中の認証同期を待たずに現在セッションを即時再評価し、旧・新両アカウントの通知購読と役割ライフサイクルを停止する。重複した認証消失イベントは一件の再ログイン案内へ集約する。
- 旧・新の読取が後から正常応答しても画面・購読・DOMを復活させず、状態変更をロックしたまま「役割選択へ戻る」だけを表示する。公開状態には両アカウント参照を含めない。
- 判定テスト343件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 偽イベント／DOMと依存注入サービスによるNode受入であり、実ブラウザの履歴・タップ、実認証SDK、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。
- 次の一項目は、この再ログイン案内から役割選択へ戻り、第三の新セッションで再入場しても旧二世代が復活しない往復受入とする。

## 今回完了 — 集約入場中の認証アカウント切替分離（2026-09-19・フィジー日付）

- 同じ運転手画面への二重選択・重複ハッシュ変更を一件へ集約している途中で、認証アカウントが切り替わる統合受入を追加した。
- 旧アカウントの現在状態読取が未完了でも旧通知購読を先に解除し、新アカウント専用の読取・購読を各一件だけ開始する。連続した認証変更イベントから同じ新アカウント処理を重複開始しない。
- 旧読取が後から正常応答しても `stale_navigation` として破棄し、新アカウントの役割画面、通知購読、DOMリスナーを上書きしない。公開状態には旧・新いずれのアカウント参照も含めない。
- 判定テスト342件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 偽イベント／DOMと依存注入サービスによるNode受入であり、実ブラウザのタップ・履歴、実認証SDK、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。
- 次の一項目は、新アカウントへの置換入場中にその認証も消失した場合、旧・新両世代を停止して再ログイン案内一件へ収束する統合受入とする。

## 今回完了 — 二重役割選択と重複ハッシュ変更の一件集約（2026-09-19・フィジー日付）

- 役割選択から運転手画面へ入場している途中に、同じ役割の二重選択と重複したハッシュ変更を重ねる統合受入を追加した。
- ルーター要求が複数届いても、同じ認証セッションの処理中入場へ合流し、役割ライフサイクル、認可済み現在状態読取、通知購読を各一件だけ開始する。古いルーター結果は `stale_navigation` として破棄し、最後の運転手ページだけを有効にする。
- 公開状態へ認証セッション参照を出さず、認証イベントとDOM操作のリスナーも一件のままであることを検査した。判定テスト341件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 偽イベント／DOMと依存注入サービスによるNode受入であり、実ブラウザのタップ・ハッシュ履歴、実認証SDK、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。
- 次の一項目は、集約された再入場中に認証アカウントが変わった場合、旧入場を破棄して新セッションだけを開始する統合受入とする。

## 今回完了 — 再入場失敗後の認証イベント再試行ループ停止（2026-09-19・フィジー日付）

- 認証・接続を確認して明示的に役割へ再入場してもライフサイクル開始が再度失敗した場合、`entry_failed` のまま安全停止し、認証変更イベントから同じ入場を自動再試行しないようにした。
- 失敗中は「役割選択へ戻る」一件だけを維持する。認証イベントが連続しても入場処理、失敗描画、DOMリスナーを増やさず、役割選択へ戻って明示的に選び直した場合だけ次の一回を開始する。
- 初回失敗と明示再入場後の再失敗を往復させ、各失敗時の認証イベント連打、ルーター版、公開状態、DOMロックを検査する統合受入を追加した。判定テスト340件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 実認証SDK／認証イベント、実ブラウザ／支援技術、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。
- 次の一項目は、役割選択からの再入場中に二重タップと重複ハッシュ変更が重なっても、入場・購読を一件へ集約する統合受入とする。

## 今回完了 — 失敗案内から役割選択を経た安全な再入場（2026-09-19・フィジー日付）

- 役割画面の認証・入場失敗案内から「役割選択へ戻る」を実行すると、案内ボタンのリスナーと旧DOM世代を解除し、利用者／運転手の状態変更ボタンに残っていた `disabled` と `aria-disabled` のロックも解除するようにした。
- 役割選択へ戻った後、認証・接続が復旧した同じ役割へ入り直すと、入場を新しい試行として開始する。旧失敗バナー、旧操作、旧セッション参照を復活させず、成功した役割画面だけを有効にする。
- DOM離脱の回帰受入と、失敗→役割選択→同じ運転手画面への再入場を通す統合受入の2件を追加した。判定テスト339件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 実認証SDK／トークン失効、実ブラウザ／支援技術、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。
- 次の一項目は、認証復旧後の明示再入場が再び失敗した場合に自動ループせず、一件の安全案内へ戻る往復受入とする。

## 今回完了 — 切替先の認証・入場失敗時の安全停止（2026-09-19・フィジー日付）

- 利用者から運転手など別役割へ切り替えた際、切替先の認証セッションが存在しなければ、旧役割へ戻さず切替先画面で通信・状態変更を停止する。新規運転手の受付・提示等も有効化しない。
- 設定済みサービスのライフサイクル入場が失敗した場合を `entry_failed` として分離した。「接続サービスは未設定」という誤案内をやめ、「役割画面を開始できません」と表示して役割選択への復帰だけを許可する。
- 切替先の入場処理へ認証同期が合流した後に失敗しても、入場処理と停止描画は各一件だけで、旧役割の画面・購読・操作を復活させない。
- 統合受入2件と既存入場失敗受入の更新を含め、判定テスト337件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 実認証SDK／トークン失効、実ブラウザ、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。

## 今回完了 — 認証同期中の役割遷移優先制御（2026-09-19・フィジー日付）

- 役割選択、下部メニュー、別役割への明示遷移を、保留中の認証同期より優先するルーター世代を追加した。明示遷移時に旧同期の保留フラグと所有権を破棄する。
- 役割選択へ戻った後に旧セッションの入場が完了しても、旧役割へ再入場せず、画面・ランタイムとも役割未選択を維持する。
- 利用者の認証同期中に運転手へ切り替えた場合、旧利用者同期を破棄し、運転手の認証イベントは新しい同期世代で処理する。旧同期が新役割のイベントを吸収したり、完了後に元の役割へ巻き戻したりしない。
- 統合受入2件を追加し、判定テスト335件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 実認証SDK／トークン失効、実ブラウザ履歴、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。

## 今回完了 — 連続認証変更の集約と入場完了境界（2026-09-19・フィジー日付）

- 同一マイクロタスク内の認証変更イベントを一件へ集約し、新セッション入場中に届いた追加イベントは最大一件だけ保留して完了後に再評価する。イベント連打で購読・起動時復旧を重複開始しない。
- 同じ役割・同じ認証セッションへの入場要求が既に処理中の場合は、その処理へ合流する。ライフサイクルの成功前に `within_role` として成功扱いせず、失敗時も全呼出側へ同じ安全な結果を返す。
- 旧セッションの入場が遅れて失敗しても、後から成功した新セッションのライフサイクル、購読、画面を停止しない。公開状態に旧・新セッション参照を含めない。
- 受入3件を追加し、判定テスト333件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 実認証SDK／トークン失効、実ブラウザ、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。

## 今回完了 — 認証セッション消失と安全な再入場の統合受入（2026-09-19・フィジー日付）

- 役割ページルーターへ `fiji:auth-session-changed` を一件だけ接続し、現在ページの役割を `sessionForRole` で再評価する経路を追加した。イベント本文の役割・セッション値は無視し、注入サービスが返す現在値だけを使う。
- 再接続後の照合中にセッションが消失した場合、旧購読・起動時イベント・DOM操作を先に解除し、遅れて完了した旧照合を描画しない。状態変更をロックし、「役割選択へ戻る」以外の操作を出さない。
- 新しい認証セッションが得られた場合は、同じ役割でも新しいライフサイクル／購読世代へ入り直し、停止案内を解除する。公開状態に旧・新セッション参照やイベント本文を含めない。
- 統合受入2件を追加し、判定テスト330件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 実認証SDK／トークン失効、実ブラウザ、実HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。

## 今回完了 — ルーターから通知再接続までの統合受入（2026-09-19・フィジー日付）

- `createRolePageRouter` → `createRolePageRuntime` → 許可メソッド限定アダプター → 共通DOM操作ボタンを一つの実行可能な偽サービス／偽DOM受入で接続した。
- 既に表示中のページへ起動時復旧が戻る場合は再遷移せず、ルーター世代を維持する。初回通知購読失敗後の二重クリックでも、再接続と認可済み最新状態照合を各一件だけ実行する。
- 役割選択へ戻る際は操作・起動時イベントのリスナーと購読を解除する。同じ役割でアカウントを替えた場合も新規購読へ交換し、旧アカウントの遅延照合結果を描画しない。公開状態にセッション参照を出さない。
- 統合受入2件を追加し、判定テスト328件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 実ブラウザ／履歴、実認証・HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続・未検証。

## 今回完了 — 許可メソッド限定の役割サービス注入アダプター（2026-09-19・フィジー日付）

- `createRoleServiceLifecycle` を追加し、起動時復旧・表示／オンラインイベント・通知購読・共通DOMフィードバックを試作側で組み立てる構成へ変更した。
- 注入サービスは `configured` と6メソッド（セッション取得、現在状態読取、通知購読、最新状態照合、通知ヒント処理、再認証）だけを許可する。欠落または余分な公開キーは、セッション取得・通信より前に拒否する。
- 各呼出しを現在の役割・ライフサイクル世代・非公開セッション参照へ固定した。離脱時は読取／購読を中止し、イベント／DOMリスナーを解除して購読解除する。役割切替後の遅延結果は破棄する。
- サービス側へ任意のライフサイクル生成権限を渡す旧境界を廃止し、公開状態にアカウント参照・通知本文・サービス応答を含めない。
- 追加受入を含む判定テスト326件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。
- 実サービス提供者、実ブラウザ、認証・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装は未接続／未検証。

## 今回完了 — ハッシュ・下部メニュー・役割選択のルーター統合（2026-09-19・フィジー日付）

- `createRolePageRouter` を追加し、直接の `show()`、`hashchange`、下部メニュー、役割選択を一つのナビゲーション世代へ統合した。従来の単独ハッシュリスナーと直接ランタイム呼出しは削除した。
- 同じ利用者／運転手内のページ移動では注入ランタイムを再生成しない。役割選択へ戻る場合は、役割ランタイムを先に離脱してから選択画面を描画する。
- 連続遷移中に古い入場処理が完了しても、後から選んだ役割・ページを描き戻さない。役割とページが矛盾する解決結果は、描画・サービス処理前に拒否する。
- ハッシュリスナーは一件だけ登録し、ルーター解除後は今後のハッシュ変更と直接遷移を拒否する。公開状態に認証セッション参照は含めない。
- 追加受入を含む判定テスト322件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。離脱済み役割の遅延成功だけでなく遅延失敗も、現在ページの結果や表示を上書きしない。
- 偽イベント対象と依存注入ライフサイクルによるNode受入であり、実ブラウザの履歴・戻る／進む、認証SDK、HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装を検証したものではない。

## 前回完了 — 役割ページの依存注入ランタイム接続（2026-09-19・フィジー日付）

- `createRolePageRuntime` を追加し、試作の `show()` による利用者／運転手ページ入退場を、明示的に注入された役割ライフサイクルへ接続した。同じ役割・同じ認証セッションの下部メニュー移動では世代を維持する。
- サービスは `configured: true`、役割別の非公開セッション取得、ライフサイクル生成の全条件を満たす場合だけ開始する。一部だけの設定、未設定、認証セッションなしでは、購読・認証・最新状態取得を開始しない。
- 未設定時は「接続サービスは未設定です」と表示し、状態変更をロックして再接続操作を出さない。通信済み・接続済みを装うローカル成功経路は置いていない。
- 役割切替中に古い入場処理が完了しても新役割へ適用せず、公開スナップショットにセッション参照を含めない。役割選択へ戻ると注入済みライフサイクルも離脱する。
- 追加受入を含む判定テスト315件が合格（失敗0・TODO 0）。API静的契約9操作とローカルHTTP契約群も合格した。設定済みとされたライフサイクル自体が入場を拒否した場合も、安全な停止へ戻して次の画面移動で使い回さない。
- 実サービス提供者は未設定であり、実ブラウザ、認証SDK、HTTP・DB、Push／WebSocket、Android実機、別端末同期、Claude本番実装を検証したものではない。

## 前回完了 — 通知購読ライフサイクルと試作DOMの統合（2026-09-19・フィジー日付）

- 役割画面ライフサイクルが所有する通知購読、共通フィードバック、試作DOMを一組にする `createNotificationSubscriptionDomBridge` を追加した。購読側は実ボタンへ別のリスナーを重ねず、DOM接続層だけが一件のクリック入口を持つ。
- 初回接続失敗の「通知を再接続する」から、再接続後の認可済み最新状態確認までを、現在の役割・認証セッション・DOM世代に一致する実ボタン経路へ接続した。二重操作は購読・照合前に拒否する。
- 役割離脱ではDOMリスナーを先に解除してバナーを隠し、購読を破棄する。役割切替中の旧照合完了は新役割のDOMへ描画せず、同役割の別アカウント再入場も新しい購読・DOM世代へ交換する。
- 公開状態は接続・ロック・操作・DOM世代だけとし、セッション参照、認証情報、通知本文、照合本文を含めない。
- これはDOM互換の偽要素と依存注入サービスによるNode受入であり、実Push／WebSocket、認証済みHTTP、実ブラウザ、Android実機の検証ではない。

## 前回完了 — 共通フィードバックの試作DOM接続（2026-09-19・フィジー日付）

- `#command-feedback`、タイトル、本文、操作ボタン、`data-command-action` の各試作DOM要素へ、役割別フィードバック状態を一か所から反映する接続層を追加した。再接続／最新状態確認／再ログイン以外の操作名は表示・実行しない。
- 通知復旧中は操作ボタンを無効化し、バナーの `aria-busy` と各ボタンの `aria-disabled` を視覚的なロック状態と同期する。利用者／運転手の状態変更ボタンも同じロックへ連動する。
- 役割画面から離れるとクリックリスナーを解除してバナーを隠す。同じ役割へ再入場した場合は新しいDOM世代として一件だけ再接続し、旧クリックや離脱後に完了した処理は描画しない。
- 実際のHTML試作はこの接続層で既存バナーを描画する。通知購読・認証・最新状態取得は引き続き依存注入した受入モデルであり、実Push／WebSocketや本番APIへ接続していない。
- これはDOM互換の偽要素によるNode受入であり、実ブラウザ操作、スクリーンリーダー、Android実機での合格ではない。

## 前回完了 — 通知購読フィードバックと共通ボタンの接続（2026-09-19・フィジー日付）

- 通知購読の接続中、再接続案内、最新状態確認、再ログイン、対象なしを既存の役割別共通フィードバックへ同期する接続層を追加した。「通知を再接続する」を共通ボタンの許可操作へ追加した。
- 共通ボタンは現在の役割・ライフサイクル世代・Command UI世代がすべて一致する場合だけ操作できる。再接続から通信障害後の最新確認まで同じボタンで一件ずつ進める。
- 二重クリックを処理前に拒否し、役割切替・同役割の別セッション再入場では旧リスナーを先に解除する。旧再ログインや旧照合の遅延完了は新画面を描画しない。
- 404は現在の運転手画面だけを「現在の運行はありません」へ解除し、旧ボタンや利用不能になった操作は再接続・読取・再認証を開始しない。
- これは偽DOMイベント対象、依存注入した購読・照合・再認証、役割画面モデルによるNode受入であり、実DOM、実認証SDK、実HTTP、Push／WebSocket、ブラウザ／Android実機を検証したものではない。

## 前回完了 — 通知復旧操作の役割画面ライフサイクル統合（2026-09-19・フィジー日付）

- 通知購読ブリッジを現在の役割・認証セッション世代の所有物として入場時に一件だけ生成し、重複入場では再購読しないようにした。
- 再接続、最新状態確認、再ログインの各操作を現在の役割・世代からだけ実行できる境界へ統合した。別役割、旧世代、離脱後、利用不能な操作は通信・認証処理前に拒否する。
- 二重操作は一件目の処理中に拒否し、役割切替・同役割の別セッション再入場では旧購読を先に解除する。旧操作の遅延完了は新しい役割画面のロックや案内を変更しない。
- 再ログイン要求は同じ世代で一度だけ開始し、完了後も新しい認証セッションで役割画面へ入り直すまで操作ロックを維持する。404による空状態解除も現在世代だけへ適用する。
- これは依存注入した購読・照合・再認証と役割画面モデルによるNode受入であり、実DOMルーター、実認証SDK、実HTTP、Push／WebSocket、ブラウザ／Android実機を検証したものではない。

## 前回完了 — 再接続後照合の結果分類と安全な解除条件（2026-09-19・フィジー日付）

- 通知トランスポートの再接続後に行う認可済み最新状態照合を、成功、通信障害、401／403、404、想定外応答へ分類するようにした。再接続しただけでは状態変更ロックを解除しない。
- 通信断・429・5xxでは利用者「依頼」／運転手「運行」の「最新状態を確認する」を一回だけ提示し、二重押下と追加自動照合を拒否する。明示照合が成功した場合だけ解除する。
- 401／403では古い状態を信頼せず再ログイン案内だけを提示してロックを維持する。404では古い対象がないものとして役割別の空状態へ安全に解除する。
- 200でも明示的な `verified: true` がない応答は採用せず、応答本文・乗車ID・資格情報を公開状態へ出さない。想定外応答は操作ロックを維持して自動ループを停止する。
- これは依存注入した照合結果とUI状態モデルによるNode受入であり、実認証SDK、実HTTP、Push／WebSocket、ブラウザ／Android実機、別端末同期を検証したものではない。

## 前回完了 — 通知購読失敗・切断時の一回限定再接続（2026-09-19・フィジー日付）

- `createRecoverableNotificationSubscriptionBridge` を追加し、通知購読の初回接続失敗または接続後切断時に、利用者は「依頼」、運転手は「運行」の状態変更操作をロックするようにした。
- 自動再接続は行わず、「通知を再接続する」の明示操作を一回だけ許可する。二重操作、再接続失敗後の再試行、切断後の旧通知読取を通信前に拒否する。
- 再接続が成功しても直ちに操作を解除せず、認証済み役割から最新状態を一回照合し、`verified: true` の場合だけ解除する。照合失敗時はロックを維持し、自動ループしない。
- ログアウト・役割離脱中の再接続を中止し、遅れて成功した旧購読を即時解除する。公開状態と役割別フィードバックへセッション参照、資格情報、接続先、中止シグナル、通知／応答本文を出さない。
- これは依存注入した購読・照合・描画によるNode受入であり、実認証SDK、Push／WebSocket、OS通知、実ネットワーク再接続、ブラウザ／Android実機を検証したものではない。

## 前回完了 — 非同期通知購読の中止と遅延接続破棄（2026-09-19・フィジー日付）

- 役割・認証セッション世代ごとに非同期通知購読を一件だけ開始し、接続処理へ中止シグナルを渡す `createAsyncNotificationSubscriptionBridge` を追加した。同一セッションの重複入場は購読を増やさない。
- ログアウト、役割切替、同役割のアカウント切替時は接続中の旧購読を中止する。中止後に旧接続が成功しても返された解除処理を即時実行し、旧購読を復活させない。
- 離脱済み購読の旧コールバックは認可済み読取へ進まず、通知読取中にセッションが切り替わった場合も遅延結果を `bridge_stale` として破棄して新画面を変更しない。
- 公開状態は接続・処理中・件数・最終結果だけとし、セッション参照、資格情報、接続先、中止シグナル、通知本文を含めない。
- これは依存注入した非同期購読とNode受入による確認であり、実認証SDK、トークン失効、Push／WebSocket接続、OS通知、実ネットワーク切断を検証したものではない。

## 前回完了 — 通知購読の認証セッション分離（2026-09-19・フィジー日付）

- 役割画面への入場に非公開の `sessionBinding` を任意で結び、通知購読・起動時復旧・フィードバックを同じ認証セッション世代へ固定した。セッション参照はライフサイクル／通知ブリッジの公開状態へ出さない。
- 同じ役割・同じセッションの重複入場は購読を増やさない。同じ役割でもアカウント／認証セッションが変わった場合は、旧通知リスナーを先に解除して新しいライフサイクル世代を作る。
- ログアウト相当の離脱後は通知を処理せず、旧アカウントの購読ブリッジを直接呼んでも認可済み読取へ進まない。
- 旧アカウントの通知読取が切替後に完了しても `bridge_stale` として破棄し、新セッションの案内・操作ロック・通知件数を変更しない。
- これは非公開オブジェクト参照と偽イベント対象によるNode受入であり、実認証SDK、トークン失効、Push／WebSocketの購読認可、OS通知を検証したものではない。

## 前回完了 — 通知イベント購読の役割世代固定（2026-09-19・フィジー日付）

- `createNotificationHintEventBridge` を追加し、役割画面への入場時に通知リスナーを一度だけ登録するようにした。コールバックへ現在の役割・ライフサイクル世代を固定し、通知イベント側から役割・世代を受け取らない。
- 通知イベントは `detail` の非機密3項目ヒントだけを既存の通知復旧へ渡す。イベント本体に別役割・偽世代を付けても権限判断へ使わない。
- 役割離脱・役割切替では通知リスナーを他の復旧ブリッジより先に解除する。同じ役割への再入場でも旧リスナーを再利用せず、新世代へ一つだけ付け替える。
- 旧購読の通知処理が遅れて完了しても `bridge_stale` として破棄し、新しい役割の案内・操作ロック・画面状態を変更しない。
- これは依存注入した偽イベント対象によるNode受入であり、実Push／WebSocket、配信先認可、OS通知購読、実ブラウザイベントを検証したものではない。

## 前回完了 — 通知ヒント入口の役割・世代固定（2026-09-19・フィジー日付）

- `createRoleRecoveryLifecycle.handleNotification` を追加し、非機密通知ヒントを現在入場中の役割・世代に一致する通知フィードバックブリッジだけへ渡すようにした。
- 別役割または古い世代を示す通知は、通知本文の検査・描画・認可済み読取より前に拒否する。役割画面を離脱した後の通知もブリッジへ渡さず通信しない。
- 同じ役割へ再入場した場合、旧世代の通知は拒否し、新世代に一致する通知だけを採用する。旧通知ブリッジのイベント件数も増やさない。
- 通知の認可済み読取中に役割が切り替わった場合、遅延結果を `stale_notification` として破棄し、新しい役割のバナーや操作ロックを描画しない。
- これは依存注入した通知入口と偽イベント対象によるNode受入であり、実Push／WebSocket配信、配信先認可、OSバックグラウンド、実ブラウザイベントを検証したものではない。

## 前回完了 — 通知ボタンブリッジの役割画面ライフサイクル統合（2026-09-18・フィジー日付）

- `createRoleRecoveryLifecycle` が任意の通知フィードバックボタンブリッジを役割世代ごとに一組所有し、役割入場時に一度だけ接続するようにした。同じ役割への重複入場ではブリッジやクリックリスナーを増やさない。
- 役割選択への離脱・役割切替では、旧通知ボタンブリッジを先に解除してから起動時復旧ブリッジを破棄する。離脱後のクリックは読取・再認証処理へ届かない。
- 別役割への切替では旧ブリッジの解除後に新役割の新しい世代を生成する。同じ役割へ再入場した場合も古いボタンを再利用せず、新しいリスナーを一つだけ接続する。
- 旧役割の通知再照合が処理中でも、切替後に届いた遅延結果は旧バナーを再描画せず、新役割の文言・操作ロックを上書きしない。
- これは偽イベント対象と依存注入した画面ライフサイクルによるNode受入であり、実DOMルーター、実ブラウザ履歴、実Push／WebSocket、実認証SDKを検証したものではない。

## 前回完了 — 通知フィードバックボタンと役割離脱の安全な接続（2026-09-18・フィジー日付）

- `createNotificationFeedbackEventBridge` を追加し、共通フィードバックのボタンイベントを現在の通知復旧 `refresh`／`reauth` 操作へ接続した。リスナーは役割画面世代ごとに一度だけ登録する。
- 同じボタンの二重クリックは読取・再認証処理の前に拒否し、一件の処理中は追加操作を開始しない。表示できる操作も現在の許可リスト済みフィードバックに一致するものだけに限定した。
- 役割離脱時はクリックリスナーと通知フィードバックフローを破棄する。処理中に離脱または役割切替した場合、遅れて完了した旧結果は新しい役割のバナーや操作ロックを描画しない。
- 古い役割のボタンイベントは離脱後に拒否し、再ログイン操作は新しい認証セッションが始まるまで一件ロックを維持する。
- これは偽イベント対象と依存注入した描画関数によるNode受入であり、実DOM、実ブラウザイベント、実認証SDK、実Push／WebSocket、OSバックグラウンドを検証したものではない。

## 前回完了 — 通知復旧と共通フィードバック操作の安全な接続（2026-09-18・フィジー日付）

- `createNotificationFeedbackFlow` を追加し、通知復旧の確認中、最新状態確認、再ログイン、対象なしを、既存の利用者／運転手共通フィードバック表示と操作ロックへ接続した。
- 通知復旧側で生成した「依頼」／「運行」の文言、`refresh`／`reauth` 操作、ロック状態だけを許可リスト検査後に共通表示へ反映する。乗車ID、通知本文、アカウント参照は表示状態へ追加しない。
- 「最新状態を確認する」と再ログイン操作は同じ役割世代で一件だけ実行し、二重押下を通信・再認証処理の前に拒否する。
- 処理中に役割が切り替わった場合、旧世代の遅延完了は新しい役割の案内や操作ロックを上書きしない。404の対象なしは運転手／利用者別の空状態を共通表示へ反映して安全に解除する。
- これは依存注入した通知・読取・再認証操作とUI状態モデルによるNode受入であり、実DOMボタン、実認証SDK、実Push／WebSocket、OSバックグラウンド、実ネットワークを検証したものではない。

## 前回完了 — 通知の明示再照合失敗を役割別に安全案内（2026-09-18・フィジー日付）

- 通知の自動照合2回で追いつかず、利用者／運転手が明示的に最新状態を確認した際の失敗を、通信、認証、対象なしに分けて扱うようにした。
- 通信断・429・5xxでは最大 `revision` を保持し、利用者には「依頼」、運転手には「運行」を取得できない案内と「最新状態を確認する」を表示する。操作ロックを維持し、自動再送しない。
- 401／403では端末内の古い乗車状態を消去し、役割別の再ログイン案内と `reauth` 操作だけを示す。後続通知から状態を推測せず、再認証まで状態変更をロックする。
- 404では古い乗車状態と通知目標を消去し、現在の依頼／運行がない役割別空状態へ戻して操作ロックを解除する。通信失敗後に届く通知は通信を増やさず最大版一件だけ更新する。
- これは依存注入した通知・読取とUI状態モデルによるNode受入であり、実ボタン操作、実認証、実Push／WebSocket、OSバックグラウンド、実ネットワークを検証したものではない。

## 前回完了 — 追いつかない通知復旧の役割別ロックと明示再照合（2026-09-18・フィジー日付）

- 最初の通知読取と追加照合1回を終えても認可済み応答が通知版へ届かない場合、`needsRefresh` と操作ロックを有効にして自動照合を停止するようにした。
- 利用者には「依頼」、運転手には「運行」の最新状態を確認する案内を分けて表示し、「最新状態を確認する」だけを次操作として示す。確認中も状態変更操作をロックする。
- ロック中にさらに新しい通知が届いても通信せず、本文を保持せず最大 `revision` 一件だけを更新する。明示再照合はその最大版へ一回だけ行い、追いつけばロックを解除する。
- 明示再照合でも追いつかなければロックと案内を維持し、自動ループしない。役割離脱時は保留版と旧役割のロックを破棄し、旧役割から再照合を送らない。
- これは依存注入した通知・読取とUI状態モデルによるNode受入であり、実ボタン操作、実Push／WebSocket、認証、OSバックグラウンド、実ネットワークを検証したものではない。

## 前回完了 — 通知読取中の最大版集約と追加照合制限（2026-09-18・フィジー日付）

- 通知復旧中に同じ乗車のさらに新しい通知が届いた場合、通知本文や複数件のキューを保持せず、正整数の最大 `revision` 一件だけを内部保留するようにした。
- 先行する認可済み読取が保留中の最大版まで取得できた場合は追加通信を送らない。届かなかった場合だけ、その最大版を目標とする認可済み読取を一度追加する。
- 追加照合中にさらに新しい版が届いても3回目を自動送信せず、最大版だけを未解決として保持する。無限再取得や通知連打による通信増幅を防ぐ。
- 私的フィールド付き通知と別乗車通知は処理中でも保留せず、役割切替時は保留版を破棄して旧役割の追加照合を送らない。
- これは依存注入した通知と読取によるNode受入であり、実Push／WebSocketの順序・欠落、配信先認可、OSバックグラウンド、実ネットワークを検証したものではない。

## 前回完了 — 通知ヒントと役割世代の一件復旧（2026-09-18・フィジー日付）

- `createRoleNotificationRecovery` を追加し、通知を `type`・`rideId`・正整数 `revision` の3項目だけに限定した。余分な個人・車両・料金・表示情報を含む通知、別乗車、古い版、重複版は認可済み読取の前に拒否する。
- 通知からの再取得も、手動の「最新状態を確認する」と `online` 自動再照合が使う現在の役割・世代の一件制御へ接続した。通知先行／手動先行／通知先行とオンラインの重複のいずれでも読取を一件に限定する。
- 画面状態を更新できるのは認可済み読取が返す6項目の役割別表現だけとした。通知本文から状態を作らず、401／403／404は端末内の古い乗車を消去し、304は新しい状態を推測しない。
- 通知読取中に役割が切り替わった場合、遅延結果は旧役割の画面や内部状態へ入らない。公開スナップショットには乗車ID、通知本文、応答本文、アカウント参照を出さない。
- これは依存注入した通知ヒントと読取によるNode受入であり、実Push／WebSocket、配信先認可、実HTTP、OSバックグラウンド、Android実機を検証したものではない。

## 前回完了 — 手動復旧とオンライン再照合の一件化（2026-09-18・フィジー日付）

- 役割画面から生成するイベントブリッジへ、役割・世代を固定した `requestRecovery` を注入し、`online` の自動再照合も手動の「最新状態を確認する」と同じ一件制御を必ず通すようにした。
- 手動操作が先行した場合は後続オンラインイベントを、オンラインが先行した場合は後続タップを通信前に拒否し、どちらの順序でも再取得を一件に限定する。
- 再取得中に画面が非表示になった場合は認可済み結果を一件だけ保留し、復帰時に適用する。非表示中の手動タップや重複イベントで追加取得を送らない。
- `online` は再認証操作を代行せず、401／403後は明示的な再認証理由だけが一度の読取を開始する。オンライン再取得中の役割切替では旧結果を破棄し、新役割の復旧は独立して実行できる。
- これは偽イベント対象と依存注入した読取によるNode受入であり、実ブラウザのオンライン判定、実ログインSDK、実HTTP、OS通信中止、Android実機を検証したものではない。

## 前回完了 — 復旧操作の役割世代固定と二重押下防止（2026-09-18・フィジー日付）

- 役割画面ライフサイクルへ `requestRecovery`、ルーティング境界へ `recover` を追加し、「最新状態を確認する」は通信回復、「再ログイン後に確認する」は再認証理由として、現在の役割・世代からだけ実行する。
- 同じ役割世代で復旧操作が処理中の場合は二回目を通信前に拒否する。利用者／運転手の文脈、役割世代、許可された理由のいずれかが一致しない操作も読取を送らない。
- 操作中に役割選択へ戻る、または別役割へ切り替えた場合は旧フローを破棄し、遅れて届いた成功結果を画面遷移や操作ロック解除へ使わない。
- 旧役割の復旧が未完了でも、新しい役割世代では独立した復旧操作を一件だけ実行できる。旧処理の終了が新処理のロックを解除しない。
- これは依存注入した読取とルーティングモデルによるNode受入であり、実ログインSDK、実HTTP、ブラウザボタン操作、OS通信中止、Android実機を検証したものではない。

## 前回完了 — 役割内ルーティングと離脱境界（2026-09-18・フィジー日付）

- `createRoleRoutingBoundary` を追加し、利用者／運転手それぞれの下部メニューと登録画面の移動を、現在の役割画面ライフサイクルへ接続した。
- 同じ役割内のホーム、提示／依頼、履歴／運行、アカウント、登録の移動では復旧世代・フロー・イベントブリッジを作り直さない。起動時読取も一件のまま維持する。
- 役割選択へ戻る場合だけ旧役割を離脱してリスナーと処理中通信を破棄し、別役割へ入る場合は新しい世代を開始する。離脱後の遅延結果は旧画面を復活させない。
- 連続した同役割内の移動には単調増加するナビゲーション版を付け、先に開始した非同期入場の完了が後から選んだ画面を巻き戻さない。不明画面と役割ヒント不一致は状態を変えず拒否する。
- これは依存注入したルーティングモデルによるNode受入であり、実ブラウザのルーター、戻る操作、実認証、実HTTP、Android実機を検証したものではない。

## 前回完了 — 役割画面の入退場と復旧世代の分離（2026-09-18・フィジー日付）

- `createRoleRecoveryLifecycle` を追加し、利用者／運転手の役割画面へ入るたびに、その役割専用の復旧フローとイベントブリッジを一組だけ生成する。
- 同じ役割への重複入場は新しい取得・リスナーを作らない。別役割へ切り替える場合は旧ブリッジを先に解除・破棄してから新しい役割を開始し、常に3種の有効リスナーを一組に限定する。
- 役割画面を離れて同じ役割へ戻る場合も新しい世代・フローを作る。旧オンライン再照合の遅延結果、旧ページ復帰イベント、旧画面遷移を新しい画面へ適用しない。
- 非表示で役割へ入った場合は初回取得を送らず、最初のページ復帰時に一件だけ取得する。公開状態は役割、世代、入場中・処理中、最終操作だけで、アカウント参照や乗車情報を含まない。
- これは依存注入した役割画面モデルと偽イベント対象によるNode受入であり、実ブラウザのルーティング、実認証、実HTTP、Android実機を検証したものではない。

## 前回完了 — 表示・ページ復帰・オンラインイベントの安全な接続（2026-09-18・フィジー日付）

- `createStartupRecoveryEventBridge` を追加し、`visibilitychange`・`pageshow`・`online` 相当のイベントを起動時復旧フローへ接続した。開始は一度だけで、同じリスナーを二重登録しない。
- 表示復帰イベントが重なっても発見APIは一件だけ呼ぶ。`online` の連打も、許可された通信回復再照合一件へまとめ、再認証待ちを通信回復として再送しない。
- オンライン再照合中に画面が隠れた場合は結果を保留し、ページ復帰時に一度だけ適用する。役割離脱時の `detach` は3リスナーを解除してフローも破棄し、遅延結果や後続イベントによる通信・画面復活を防ぐ。
- ブリッジの公開状態は接続・処理中・最終イベント・件数だけで、アカウント参照、乗車ID、応答本文を含まない。
- これはブラウザ互換の偽イベント対象を用いたHTML内試作とNode受入テストであり、実ブラウザイベント、OSオンライン判定、実認証SDK、実HTTP、Android実機を検証したものではない。

## 前回完了 — 画面表示状態と復旧読取の一件限定制御（2026-09-18・フィジー日付）

- 起動時復旧フローへ `setVisible` を追加し、画面非表示中は起動時取得・通信回復後の再取得を送信しない。非表示中の再取得操作は試行回数を消費しない。
- 読取中に画面が隠れた場合、認可済み結果を画面へ即時適用せず、同じフロー世代の内部保留結果として一件だけ保持する。画面復帰時に一度だけ役割別画面と操作ロックへ適用する。
- 非表示のまま新セッションを初期化した場合はマーカーを保持し、最初に表示された時点で一件だけ発見APIを呼ぶ。非表示／表示が短時間に重なっても処理中の読取を重複させない。
- 役割離脱でフローを破棄した場合は保留結果も破棄し、その後の表示イベントで旧結果を復活させない。公開状態には保留本文、乗車ID、アカウント参照を出さない。
- これはHTML内の試作とNode受入テストであり、実ブラウザの `visibilitychange`、OSバックグラウンド、実認証SDK、実HTTP、Android実機を検証したものではない。

## 前回完了 — 復旧操作と役割画面の安全な接続（2026-09-18・フィジー日付）

- `createStartupRecoveryFlow` を追加し、起動時の初回取得、通信回復後の明示再取得、再認証後の明示再取得、新セッション開始を、役割別UIアダプターへ接続した。
- 利用者は履歴／利用者ホーム、運転手は運行一覧／運転手ホームだけへ復帰する。通信断・401では操作ロックを維持し、許された理由で一度再取得できた場合だけ画面遷移とロック解除を適用する。
- 役割選択へ戻る／ログアウト相当の `dispose` は処理中の取得を中止する。中止に間に合わず届いた旧結果もフロー世代で破棄し、旧画面への遷移や操作解除を防止する。
- 役割不一致、新セッションの二重初期化、不正な再取得理由はHTTP要求前に拒否する。フローの公開状態へアカウント参照、乗車ID、トークンを出さない。
- これはHTML内の試作とNode受入テストであり、実認証SDK、実HTTP、ブラウザイベント、OSバックグラウンド、Android実機、別端末同期を検証したものではない。

## 前回完了 — 起動時復旧結果の役割別UI適用（2026-09-18・フィジー日付）

- `createStartupRecoveryUiAdapter` を追加し、利用者の復旧は利用者履歴／ホーム、運転手の復旧は運行一覧／運転手ホームだけへ遷移させる。復旧結果の役割、セッション世代、試行番号、画面種別を適用前に照合する。
- 同じ復旧結果は画面遷移・ロック解除とも一度だけ適用する。完了後の重複結果、旧セッションの遅延結果、他役割の結果は無視する。
- 通信断は最新状態確認ロック、401／403は再ログインロック、競合・不正画面は競合ロックへ反映する。想定外のページや役割で操作を解除せず、安全側に停止する。
- 新しいセッション世代の開始時は適用履歴を破棄して再びロックし、古い世代の完了では解除しない。画面アダプターの状態にアカウント参照や乗車IDは保存しない。
- これはHTML内の試作とNode受入テストであり、実認証SDK、実HTTP、ブラウザ画面操作、Android実機、別端末同期を検証したものではない。

## 前回完了 — 新セッションの一度だけの起動時復旧（2026-09-18・フィジー日付）

- 新しい認証済みアカウントへ切り替えた後だけ、役割別の起動時発見を明示的に一度初期化できる `beginSessionRecovery` を追加した。同じセッションでの二重初期化と、利用者コントローラーへの運転手セッションなど役割不一致を拒否する。
- 旧セッションの未確定マーカーを消去してから、版・役割・未確定状態・新しい保存時刻だけのマーカーを作る。旧／新アカウント参照、乗車ID、トークン、コマンド、冪等性キーは保存しない。
- アカウント切替中に旧取得と新取得が重なっても、旧結果は `stale_session` として破棄し、新しい取得は一件だけ維持する。旧取得の終了処理が新取得の処理中ロックを解除しないよう、処理中判定を世代付き取得ハンドル集合から算出する。
- 新利用者の200は利用者履歴、新運転手の204は運転手ホームへ復帰し、復帰後にマーカーを消去する。状態変更コマンドの自動再送はない。
- これはHTML内の試作とNode受入テストであり、実認証SDK、OSレベルの通信中止、実HTTP、ブラウザ再読込、別端末同期を検証したものではない。

## 前回完了（再実装なし）

起動時発見のセッション世代分離、起動時復旧と未完了乗車発見の接続、認証済み役割からの未完了乗車発見、起動時照合の通信断・認証切れ復旧、再読込・再起動後の未確定操作ガード、復旧中の重複操作停止と役割別案内、結果不明コマンドの照合と一度だけの再送、状態変更中のセッション失効と冪等性分離、ログアウト・役割／アカウント切替の分離、非機密通知ヒント、通知・再取得の重複／順不同統合、再取得の待機・停止制御、条件付き状態再取得、競合後の役割別状態再取得、迎車中の取消／乗車開始HTTP競合、車両確認と運行遷移のHTTP安全契約、安全な監査イベント、サーバー時刻と資格の選択時再検証、提示選択／取消のHTTP競合・冪等再送、役割・所有者のHTTP契約、OpenAPIと静的契約検査、画面内の競合制御、提示期限切れ、予約日時、未乗車の依頼取り消し、HANDOVER-01の車両再照合は修正済み。今回もすべて回帰合格した。

## 検証

```bash
node --test prototypes/tests/verification.test.cjs prototypes/tests/role-handover.test.cjs
```

結果: **337件合格、失敗0、TODO 0**（照合20件、役割・乗車・API・HTTP契約317件）。切替先のセッション消失と入場失敗を検証する統合受入2件を追加し、既存335件も回帰合格。HTML内3スクリプトの構文検査も合格。

追加で `node prototypes/http-contract-runner.cjs` を実行し、**認証済み役割からの未完了乗車発見、14の役割・所有者シナリオ、提示／乗車開始の競合・冪等再送、結果不明コマンドの照合・一度だけの再送、競合後の安全な再取得、条件付き304、429／503／通信断の再試行制御、版による順不同統合、非機密通知ヒント、読取／状態変更のセッション分離、期限切れ境界、車両再確認、資格失効、運行遷移、安全な監査イベント17件に合格**。`node prototypes/api-contract-check.cjs` の9操作・安全な発見／条件付き読取・Retry-Afterの静的契約検査も合格した。

HTTP通信はローカルのメモリ内モックに対するもの。本番認証プロバイダー、永続DB・ロック、永続Idempotency-Key、複数サーバー、外部APIや別端末の合格ではない。遠隔URLや実トークンは設定・使用していない。

これらはローカルの判定モデルのテスト。ブラウザのURL安全設定による前回のブロック以降、画面操作・360/390/430px表示の再検証は行っていない。ブラウザ互換イベントを偽対象で接続した受入であり、実ブラウザイベントの合格とは扱わない。

## 次の優先項目

1. Claudeの本番実装接続を確認できたら、同じ競合・期限・資格・監査・再取得契約を認証・永続DB付き環境で実行する。未接続の間は、切替先の失敗案内から役割選択へ戻り、認証復旧後に同じ役割へ再入場した際、旧失敗表示を消して新しい世代だけを開始する往復受入を追加する。

## 本番接続の状態

実ログイン、DB保存、別端末同期、本人・許可照会、書類保存、実配車、決済は未接続。Google Maps実通信は未検証。新規運転手の申請だけで受付・提示・迎車を許可しない。

Claudeを直接起動する手段は、接続済みツール・実行環境・プラグイン検索で確認できていない。接続を確認できた場合のみ本番実装を渡し、実行結果をGitHubで確認する。それまではGDP側の試作・設計・レビューを進める。

## 継続開発の進め方

- GitHubの最新PR・コミット・この文書を読み、別の作業が更新されていればその変更を引き継ぐ。
- 未完了の優先項目を一つ進め、意味のある検証を実行してから作業ブランチへ保存する。
- 実際に変更した内容、検証結果、未接続部分、次の一項目を記録する。変更のない報告だけのコミットは作らない。
- 既存のPRを更新し、競合時は他の変更を保持する。強制pushや無関係なファイル変更はしない。
- 本番統合・公開、実在の免許情報、課金を伴うAPI利用を、試作の完了と混同しない。公開の前に実際の接続・検証状態を確認する。
- ブラウザや接続の安全設定を回避せず、利用可能な判定テスト・設計・レビューを進める。
